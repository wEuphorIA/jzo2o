<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jzo2o.market.mapper.ActivityMapper">

    <select id="queryList" resultType="com.jzo2o.market.model.dto.response.ActivityInfoResDTO"
            parameterType="com.jzo2o.market.model.dto.request.ActivityQueryForPageReqDTO">
        select *
        FROM
        activity
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="name != null and name != ''">
                AND name like CONCAT('%',#{name},'%')
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        order by update_time desc
    </select>
</mapper>
